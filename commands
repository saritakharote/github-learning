git config --global user.email"kharote.sarita@gmail.com"
git config --global user.name"saritakharote"

learn git part-1:

1)git init
2)git add[filename]|| -A
git status
git commit -m "msg"
git remote add origin reponame
git push origin master
git clone reponame
git pull origin master